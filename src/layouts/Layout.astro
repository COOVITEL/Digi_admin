---
import AsideMenu from "../components/AsideMenu.astro"
interface Props {
	title: string;
}

const { title } = Astro.props;

---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Datos Digiturno - En esta web podras encontrar los datos recopilados en las diferentes sucursales de Coovitel." />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="./logoup.png" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<div id="login"
			class="h-[620px] w-full flex flex-col items-center justify-center
			bg-gradient-to-t from-[#2c3870] to-black hidden">
			<img src="./logoup.png" alt="logo" class="relative bottom-6 w-26 h-28">
			<div class=" flex flex-col gap-5 items-center justify-center
				w-[450px] h-[420px] rounded-md bg-black/70 text-white">
				<h1 class="text-white relative bottom-5 text-3xl font-serif">Datos Digturno</h1>
				<div class="flex flex-row justify-center items-center">
					<label>
						<input
						type="text"
						placeholder="Usuario"
						id="username"
						required
						class="bg-transparent border-b-2 focus:border-b-0 px-5 py-2">
					</label>
					<img class="invert h-6 w-6 ml-4" src="./user.png" alt="user">
				</div>
				<div class="flex flex-row justify-center items-center">
					<label>
						<input
						type="password"
						placeholder="Contraseña"
						id="password"
						required
						class="bg-transparent border-b-2 focus:border-b-0 px-5 py-2">
					</label>
					<img class="invert h-6 w-6 ml-4" src="./candado.png" alt="password">
				</div>
				<span class="hidden relative top-5" id="user-empty">Ingrese Usuario y Contraseña</span>
				<span class="hidden relative top-5" id="user-failed">Usuario o la Contraseña no conciden</span>
				<button id="bl" class="bg-[#2c3870] relative rounded-3xl top-10 py-2 px-6 hover:bg-[#2c2060] transition duration-200 active:scale-110">Iniciar Sesión</button>
			</div>
			<script src="./login.js"/>

		</div>

		<div id="app" class="relative h-screen p-2 gap-2 hidden">
			<aside class="[grid-area:aside] bg-zinc-800 rounded-lg flex-col flex overflow-y-auto">
				<AsideMenu />
			</aside>
	  
			<main
			  class="[grid-area:main] rounded-lg bg-zinc-800 overflow-y-auto w-full"
			>
			  <slot />
			</main>
	  
			<footer class="[grid-area:footer] bg-zinc-800 rounded-lg h-[20px]">
			</footer>
		</div>
	</body>
</html>
<style>
	#app {
		display: grid;
		grid-template-areas:
			"aside main main"
			"footer footer footer";
		grid-template-columns: 200px 1fr;
		grid-template-rows: 1fr auto;
	}
</style>
<style is:global>
	:root {
		--accent: 136, 58, 234;
		--accent-light: 224, 204, 250;
		--accent-dark: 49, 10, 101;
		--accent-gradient: linear-gradient(
			45deg,
			rgb(var(--accent)),
			rgb(var(--accent-light)) 30%,
			white 60%
		);
	}
	html {
		font-family: system-ui, sans-serif;
		background: #13151a;
	}
	code {
		font-family:
			Menlo,
			Monaco,
			Lucida Console,
			Liberation Mono,
			DejaVu Sans Mono,
			Bitstream Vera Sans Mono,
			Courier New,
			monospace;
	}
</style>
